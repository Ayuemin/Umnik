name: Apply Umnik 0.7.5 sound picker patch

on:
  push:
    branches:
      - main
    paths:
      - ".github/workflows/apply-075-sound.yml"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply patch
        run: python3 scripts/patch_075_sound.py

      - name: Commit changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/ayuemin/ymnik/model/Models.kt \
                  app/src/main/java/com/ayuemin/ymnik/data/StorageRepository.kt \
                  app/src/main/java/com/ayuemin/ymnik/ChatViewModel.kt \
                  app/src/main/java/com/ayuemin/ymnik/ui/YmnikApp.kt \
                  app/build.gradle.kts \
                  CHANGELOG.md
          git diff --cached --check
          git commit -m "Add persistent custom answer sounds for 0.7.5"
          git push origin HEAD:main
