name: Dispatch Umnik v1.14.0 Release

on:
  push:
    branches: [ "main" ]
    paths: [ ".github/workflows/dispatch-v1.14.0-release.yml" ]

permissions:
  contents: write
  actions: write

jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Create v1.14.0 tag on tested release commit
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if gh api "repos/${{ github.repository }}/git/ref/tags/v1.14.0" >/dev/null 2>&1; then
            echo "v1.14.0 already exists"
          else
            gh api --method POST "repos/${{ github.repository }}/git/refs" \
              -f ref='refs/tags/v1.14.0' \
              -f sha='2df6bcc1a2e135bbc5d65c151dabe98d1d6b0883'
          fi

      - name: Dispatch signed Android release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: gh workflow run android-release.yml --repo "${{ github.repository }}" -f tag=v1.14.0

      - name: Remove temporary dispatcher
        if: always()
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          file_sha=$(gh api "repos/${{ github.repository }}/contents/.github/workflows/dispatch-v1.14.0-release.yml?ref=main" --jq .sha)
          gh api --method DELETE "repos/${{ github.repository }}/contents/.github/workflows/dispatch-v1.14.0-release.yml" \
            -f message='Remove temporary v1.14.0 release dispatcher' \
            -f sha="$file_sha" \
            -f branch='main'
