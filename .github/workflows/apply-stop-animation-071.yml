name: Apply Umnik 0.7.1 stop animation

on:
  push:
    branches: [ "main" ]
    paths:
      - "scripts/patch_stop_animation_071.py"
      - ".github/workflows/apply-stop-animation-071.yml"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply patch
        run: python3 scripts/patch_stop_animation_071.py

      - name: Commit app changes
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/src/main/java/com/ayuemin/ymnik/ui/YmnikApp.kt CHANGELOG.md
          if git diff --cached --quiet; then
            echo "No changes"
            exit 0
          fi
          git commit -m "Animate active Stop control"
          git push origin main
