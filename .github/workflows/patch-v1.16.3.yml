name: Patch v1.16.3

on:
  push:
    branches: ["fix/background-navigation-v1.16.3"]

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: fix/background-navigation-v1.16.3
          fetch-depth: 0
      - name: Bootstrap
        run: echo "v1.16.3 patch workflow is valid"
