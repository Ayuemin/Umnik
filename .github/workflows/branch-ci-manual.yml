name: Sidebar Android Validation

on:
  push:
    branches:
      - feat/sidebar-navigation-v1.5.0

jobs:
  validation-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Android CI also validates this branch"
