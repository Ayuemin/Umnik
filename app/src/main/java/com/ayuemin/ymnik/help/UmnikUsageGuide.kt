package com.ayuemin.ymnik.help

object UmnikUsageGuide {
    private data class Topic(
        val title: String,
        val body: String
    )

    private val TOPICS = listOf(
        Topic(
            "Быстрый старт",
            """
# Быстрый старт

**Что это:** минимальная настройка для обычного чата.

**Где найти:** **Настройки → Подключение → OpenRouter**.

1. Вставьте свой API-ключ OpenRouter и сохраните его.
2. Нажмите **Проверить подключение**.
3. Откройте **Настройки → Модели** и выберите модель для обычного чата.
4. Вернитесь в чат и задайте вопрос.

Umnik не предоставляет собственную подписку на модели: используется ваш ключ OpenRouter, а стоимость зависит от выбранной модели.

**Пример:** «Объясни простыми словами, чем Wi-Fi отличается от мобильного интернета».
            """.trimIndent()
        ),
        Topic(
            "Обычные чаты",
            """
# Обычные чаты

Каждый обычный чат хранит свою переписку, файлы и индивидуальные настройки. Для новой независимой темы удобнее создать отдельный чат.

**Где найти:** боковая панель → **Новый чат**.

Модель текущего чата выбирается сверху. Часто используемые модели можно добавить в быстрые и переключать без похода в настройки.

Через меню рядом с чатом можно изменить название, закрепить роль и постоянную инструкцию, добавить чат в избранное, настроить память и базу знаний.

**Пример:** один чат можно оставить для работы с документами, другой — для бытовых вопросов, чтобы контексты не смешивались.
            """.trimIndent()
        ),
        Topic(
            "Модели и быстрый выбор",
            """
# Модели и быстрый выбор

**Где назначать модели:** **Настройки → Каталог и модели OpenRouter**.

Это единое место выбора и назначения моделей. Здесь задаются модель чата по умолчанию, быстрые текстовые модели, изображения, речь, распознавание, видео, Batch, Embeddings и другие поддерживаемые задачи.

В основной карточке **Модели**:

- **Чат по умолчанию** показывает текущую модель и позволяет сбросить её на Auto;
- **Быстрые модели** раскрывают уже выбранный список и позволяют убрать отдельную модель;
- новый поиск моделей из этих двух карточек не открывается.

Быстрые модели можно переключать прямо из чата.

Некоторые функции зависят от возможностей конкретной модели. В каталоге отдельно показано, что модель принимает на вход и что умеет выдавать: текст, изображения, аудио, видео и другие модальности.

Каталог оставлен простым: сначала выбирается **тип** модели, затем при необходимости **стоимость**. Редкие типы и специальные возможности открываются через **Больше**. Переключатель **По возможностям** сортирует уже отфильтрованные модели от большего показателя «Возможности N/100» к меньшему. Если фильтры и сортировка не выбраны, модели идут по алфавиту.

**Мультимодальный чат** теперь означает строгий набор возможностей: одна и та же модель принимает текст и изображения и умеет выдавать и текст, и изображения. Модель, которая только анализирует фотографии, под этот фильтр не попадает.

В списке остаются только основные сведения, цена и показатель **«Возможности N/100»**. Он показывает только широту функций модели: поддерживаемые входы и выходы, инструменты API, контекст и специализированные возможности. Это не оценка интеллекта, качества ответов, скорости или цены. 100/100 означает максимально широкий набор функций, который Umnik умеет распознавать в данных OpenRouter.

Полные характеристики конкретной модели открываются через **⋮ → Информация о модели**. Там показываются расшифровка показателя возможностей, разобранные Umnik данные и исходные метаданные OpenRouter, включая новые поля, которые приложение ещё не успело оформить отдельными подписями.

Например, камера доступна только если выбранная модель принимает изображения, reasoning — только если модель его поддерживает, а модель с выходом «изображение» может создать картинку прямо по просьбе в обычном чате.

**Совет:** если функция недоступна, сначала проверьте входные и выходные возможности выбранной модели в каталоге.
            """.trimIndent()
        ),
        Topic(
            "Файлы, PDF и фотографии",
            """
# Файлы, PDF и фотографии

**Где найти:** **+ → Вставить**. Для нового снимка используйте **+ → Быстрое фото**.

Файлы можно использовать вместе с перепиской. Текстовые документы и PDF подходят для анализа содержания; изображения требуют модель, которая принимает изображения.

Файл, закреплённый в чате, остаётся его рабочим материалом для следующих сообщений.

**Пример:** прикрепите договор, спросите «Кратко перечисли мои обязанности», а затем уточните «Какой срок уведомления указан в документе?».
            """.trimIndent()
        ),
        Topic(
            "Размышление и поиск",
            """
# Размышление и поиск

**Где найти:** меню **+** текущего обычного чата.

**Размышление** доступно только у моделей, которые поддерживают reasoning. Переключатель включает или выключает его, а нажатие на сам блок **Размышление** открывает уровни, которые OpenRouter сообщает именно для текущей модели. Если модель не позволяет выбирать уровень, Umnik так и показывает: силу размышления определяет сама модель.

**Поиск** использует современный серверный инструмент OpenRouter **web_search**, а не старый web-plugin. Нажмите на блок **Поиск**, чтобы выбрать режим:

- **По необходимости** — модель сама решает, когда нужен интернет;
- **Быстрый** — короткий поиск, до 3 результатов;
- **Обычный** — до 5 результатов за поисковое обращение;
- **Глубокий** — несколько поисковых шагов для сложных вопросов; обычно работает дольше и может стоить дороже.

Сервис поиска и другие технические параметры: **Настройки → Подключение → OpenRouter → Инструменты и веб-поиск**.

**Пример размышления:** «Сравни три варианта и объясни риски каждого».

**Пример поиска:** «Какая сейчас актуальная версия этой программы?».

Для давно известных фактов поиск обычно не нужен.
            """.trimIndent()
        ),
        Topic(
            "Память и длинные чаты",
            """
# Память и длинные чаты

Umnik умеет работать с длинной перепиской, не отправляя основной модели весь многомесячный чат при каждом сообщении.

**Где выбрать режим конкретного чата:** боковая панель → **⋮ → Настройки чата → Контекст чата**.

Режимы:

- **По умолчанию** — наследует общую настройку;
- **Баланс** — до порога использует полную историю, затем оставляет до 5 последних пар и собирает свежую историю, общий конспект и найденную память в бюджет до 16 000 токенов;
- **Всегда полный** — старается передавать максимум исходной истории, который помещается в окно модели;
- **Эконом** — раньше включает гибридную память, оставляет до 3 последних пар и использует бюджет до 6 000 токенов.

По умолчанию гибридная память включается после примерно 30 000 токенов в режиме **Баланс** и после 5 000 в режиме **Эконом**.

Общий конспект ограничен 3000 знаками. Поиск возвращает максимум 3 старых фрагмента в **Балансе** и 2 в **Экономе**; соседний фрагмент, если он нужен, входит в этот общий лимит.

Старая пара индексируется сразу после выхода из свежего хвоста. Порог checkpoint 8000 токенов используется отдельно: он определяет, когда обновлять общий конспект, но не задерживает появление пары в поисковой памяти.

**Общие параметры:** **Настройки → Чаты и контекст → Память и контекст**.

Для смыслового поиска по старой истории используются Embeddings. Служебная память хранится отдельно от переписки: очистка памяти не удаляет сообщения.
            """.trimIndent()
        ),
        Topic(
            "Навыки обычного чата",
            """
# Навыки обычного чата

Навык — сохранённая инструкция и текстовые материалы, которые можно подключать по необходимости.

**Библиотека:** **Настройки → Чаты и контекст → Навыки**.

Небольшой навык можно создать прямо здесь: введите текст в поле **«Короткий навык»** и нажмите **«Создать короткий навык»**. Большие навыки по-прежнему можно импортировать файлом или папкой.

**Включить в текущем чате:** **+ → Навыки**.

Навыки обычных чатов не являются навыками агентов проекта. У агентов свои независимые навыки.

**Пример:** подключите навык со стилевыми правилами перед написанием статьи и оставьте его выключенным в других чатах.
            """.trimIndent()
        ),
        Topic(
            "База знаний обычного чата",
            """
# База знаний обычного чата

База знаний нужна для больших документов, которые неудобно прикладывать целиком к каждому запросу.

**Где найти:** боковая панель → **⋮ → Настройки чата → База знаний чата**.

При индексации Umnik извлекает текст, делит его на фрагменты и получает Embeddings через OpenRouter. Исходник, фрагменты и векторы хранятся локально. При новом вопросе основной модели передаются только найденные подходящие фрагменты.

База знаний не полностью офлайн: текст при индексации и поисковый запрос передаются в OpenRouter Embeddings.

Для сканированных PDF без текстового слоя автоматического OCR пока нет.
            """.trimIndent()
        ),
        Topic(
            "Что такое проект",
            """
# Что такое проект

В Umnik проект — это **кабинет для Оркестратора и независимых агентов**.

У самого проекта нет общей инструкции, общей библиотеки навыков или общей базы знаний, которые автоматически наследуются всеми агентами.

**Где найти:** боковая панель → **Проекты** → создать или открыть проект.

Внутри проекта есть:

- один Оркестратор;
- любое количество специалистов;
- отдельный чат каждого агента.

Настройки и знания хранятся у конкретного агента. Это уменьшает случайное смешивание ролей и контекста.
            """.trimIndent()
        ),
        Topic(
            "Создание и настройка агента",
            """
# Создание и настройка агента

**Где найти:** проект → **Новый агент**.

У специалиста можно настроить:

- имя и роль;
- личную инструкцию;
- основную модель;
- быстрые модели;
- модель контекста;
- Embeddings памяти;
- reasoning и его уровень;
- веб-поиск;
- собственные навыки;
- постоянные файлы;
- собственную базу знаний.

После сохранения можно открыть обычный чат этого агента и работать с ним напрямую.

**Важно:** имя агента не является командой для движка. Можно назвать специалиста «Редактор», «Третий чувак» или как угодно. Umnik различает агентов по внутреннему идентификатору, а Оркестратор ориентируется на их роль, инструкцию и текущую задачу.
            """.trimIndent()
        ),
        Topic(
            "Изоляция агентов",
            """
# Изоляция агентов

Каждый агент проекта имеет собственную рабочую среду.

Другой агент автоматически не получает:

- его постоянные файлы;
- его навыки;
- его базу знаний;
- его память;
- его историю разговора;
- его настройки модели.

Настройки обычных чатов также не наследуются агентами.

Когда одному специалисту нужен результат другого, Оркестратор передаёт рабочий результат явно. Это не открывает следующему агенту всю чужую память или библиотеку.
            """.trimIndent()
        ),
        Topic(
            "Оркестратор",
            """
# Оркестратор

Оркестратор — специальный управляющий агент проекта. Его задача — понять поручение пользователя, выбрать специалистов, выдать им задания, дождаться результатов и решить следующий шаг.

**Где найти:** проект → **Оркестратор → Открыть Оркестратора**.

Поручение можно писать обычным языком.

**Пример:** «Пусть два специалиста независимо разберут материал, потом передай оба результата третьему для объединения и верни итог».

Оркестратор не должен выполнять работу специалиста вместо него, если в проекте есть подходящий агент. Каждый вызванный специалист сохраняет своё поручение и ответ в собственном чате.
            """.trimIndent()
        ),
        Topic(
            "Параллельная и последовательная работа",
            """
# Параллельная и последовательная работа

Если несколько поручений не зависят друг от друга, Оркестратор может запустить их одновременно.

Если следующему агенту нужны результаты предыдущих, такой шаг запускается только после их завершения.

Пример:

1. Исследователь A и Исследователь B работают параллельно.
2. Синтезирующий агент получает оба результата.
3. Проверяющий агент получает материалы, которые нужны ему для независимой проверки.

Внутри одного и того же агентного чата одновременно выполняется не более одной работы, поэтому его история не должна смешиваться с другой задачей.
            """.trimIndent()
        ),
        Topic(
            "Передача результатов между агентами",
            """
# Передача результатов между агентами

Результат каждого поручения сохраняется как отдельный рабочий результат.

Оркестратор решает, какие результаты передать следующему специалисту. Передача результата не означает передачу всей личной истории предыдущего агента.

Если следующему агенту поручено проверить вывод относительно нескольких предыдущих работ, Оркестратор должен передать ему все необходимые результаты, а не только последний пересказ.

Так можно строить цепочки анализа, написания и проверки, сохраняя отдельную рабочую среду каждого специалиста.
            """.trimIndent()
        ),
        Topic(
            "Предпусковая диагностика проекта",
            """
# Предпусковая диагностика проекта

Перед первым запуском проекта и после изменения его конфигурации Umnik выполняет локальную проверку. Для этого не вызывается ИИ-модель и не расходуется баланс OpenRouter.

Проверяются критичные настройки Оркестратора и специалистов: наличие основной модели, подключения, API-ключа и других обязательных данных. Также могут быть показаны некритичные замечания, например о пропавшем файле или пустой инструкции.

Если всё готово, появляется сообщение:

**✓ Диагностика проекта пройдена**

Если есть блокирующая проблема, работа не запускается, а в чате объясняется, какой агент требует настройки и что исправить.

После изменения конфигурации проверка выполняется снова.
            """.trimIndent()
        ),
        Topic(
            "Если один агент завершился ошибкой",
            """
# Если один агент завершился ошибкой

Ошибка одного поручения не должна уничтожать уже выполненную работу других специалистов.

Umnik сохраняет успешные результаты и статус неудачного поручения. Оркестратор получает информацию о том, какая задача завершилась, какая упала и почему.

Уже завершённый платный результат не следует автоматически выполнять повторно. Оркестратор может продолжить с имеющимися результатами, попросить исправить настройку или решить, нужен ли повтор только неудачного шага.
            """.trimIndent()
        ),
        Topic(
            "Очистка разговоров агентов",
            """
# Очистка разговоров агентов

Историю разговора агента можно очистить, не удаляя самого специалиста.

При очистке сохраняются его постоянные настройки: имя, роль, модели, инструкция, навыки, файлы, база знаний и настройки памяти.

Удаление самого агента — другое действие: вместе с ним удаляется его локальная рабочая среда. Оркестратор удаляется только вместе с проектом.
            """.trimIndent()
        ),
        Topic(
            "Фоновая работа, UIDT и восстановление",
            """
# Фоновая работа, UIDT и восстановление

На Android 14 и новее пользовательские модельные запросы запускаются через штатный механизм **User-Initiated Data Transfer (UIDT)** и JobScheduler. На более старых Android используется совместимый foreground-механизм.

Android или прошивка производителя всё равно могут остановить работу. Поэтому Umnik отдельно хранит служебное состояние запроса и старается безопасно восстановить уже начатую работу.

Для прямого OpenRouter-запроса Umnik не делает скрытый повтор оплачиваемого POST после неопределённого обрыва. Если доступен безопасный read-only способ получить уже существующий результат, используется он. Иначе приложение сообщает о прерывании, а решение о новом платном запросе остаётся пользователю.

Во время активной работы можно переходить в другие чаты; результат должен вернуться в исходный чат.
            """.trimIndent()
        ),
        Topic(
            "Создание изображений",
            """
# Создание изображений

Если текущая модель чата умеет выдавать и текст, и изображения, отдельный режим не нужен: просто напишите в обычном чате «Создай изображение…», «Нарисуй…» или другой прямой запрос на картинку. Umnik запросит изображение у этой же модели и покажет его в ответе.

Отдельный путь **+ → Создать изображение** остаётся для специализированной модели изображений и ручного выбора параметров. Если выбранная модель принимает изображение на вход, можно добавить референс.

Готовое изображение возвращается в исходный чат. Нажмите на него, чтобы открыть на весь экран; масштабируйте двумя пальцами или двойным нажатием.
            """.trimIndent()
        ),
        Topic(
            "Речь и голос",
            """
# Речь и голос

- Микрофон в поле ввода позволяет записать голосовое сообщение, если текущая модель принимает аудио.
- **+ → Инструменты OpenRouter → В текст** запускает распознавание речи.
- **+ → Инструменты OpenRouter → Озвучить** создаёт речь через выбранную Speech-модель.
- Кнопка **OR** под ответом быстро озвучивает готовый текст выбранной OpenRouter Speech-моделью.
- Локальная Android TTS может использоваться для обычного прослушивания ответа.

Конкретные возможности зависят от выбранной модели.
            """.trimIndent()
        ),
        Topic(
            "Batch, видео и Shell",
            """
# Batch, видео и Shell

Эти функции находятся в **+ → Инструменты OpenRouter**.

**Пакет задач (Batch)** предназначен для нескольких независимых задач. Результат одной Batch-задачи автоматически не становится входом другой. Для зависимой многошаговой работы используйте Оркестратор и агентов.

**Видео** запускает генерацию ролика поддерживаемой моделью.

**Shell** используется для задач, где модели полезны вычисления, команды или создание рабочих файлов в поддерживаемой среде OpenRouter.

Асинхронный результат должен возвращаться в тот чат, из которого задача была запущена.
            """.trimIndent()
        ),
        Topic(
            "Embeddings, RAG и Rerank",
            """
# Embeddings, RAG и Rerank

**Embeddings** используются для смыслового поиска по истории и базе знаний.

**RAG** добавляет найденные подходящие фрагменты в контекст основной модели.

**Rerank** может дополнительно пересортировать найденные результаты, если такая функция и модель выбраны.

Модели для этих задач выбираются через каталог OpenRouter и настройки конкретной базы знаний.
            """.trimIndent()
        ),
        Topic(
            "Коротко обо мне",
            """
# Коротко обо мне

**Где найти:** **Настройки → Чаты и контекст → Коротко обо мне**.

Это необязательный профиль с именем, возрастом, родом занятий и короткой установкой.

У профиля один переключатель: **использовать в обычных чатах — Вкл/Выкл**.

Профиль не передаётся Оркестратору, агентам и другим проектным запросам. Он не нужен для работы Umnik и может оставаться пустым.
            """.trimIndent()
        ),
        Topic(
            "Интерфейс и звук",
            """
# Интерфейс и звук

**Где найти:** **Настройки → Интерфейс**.

Можно выбрать цветовую схему приложения, задать свой HEX-цвет и настроить сигнал готового ответа.

Для сигнала можно использовать встроенный вариант или добавить собственный аудиофайл, который копируется в локальное хранилище Umnik.
            """.trimIndent()
        ),
        Topic(
            "Хранилище, приватность и диагностика",
            """
# Хранилище, приватность и диагностика

**Хранилище:** **Настройки → Данные → Хранилище Umnik**. Нажатие на этот пункт сразу открывает рабочие файлы.

В пользовательском хранилище показываются созданные, сохранённые и экспортированные рабочие файлы. Внутренние системные данные приложения здесь не отображаются.

**Диагностика:** **Настройки → Диагностика и о приложении → Диагностика и логи**. Логирование включается вручную для поиска ошибок.

Диагностический журнал фиксирует технические события: сетевые стадии, выбранную модель, типы вложений, фоновые задания и статусы. Тексты сообщений, содержимое пользовательских файлов и API-ключи не должны записываться.

API-ключ OpenRouter хранится локально с использованием Android Keystore. Памятка работает локально и не расходует баланс OpenRouter.
            """.trimIndent()
        ),
        Topic(
            "Если что-то не запускается",
            """
# Если что-то не запускается

Для обычного чата проверьте:

1. сохранён ли API-ключ OpenRouter;
2. проходит ли **Проверить подключение**;
3. выбрана ли подходящая модель;
4. поддерживает ли модель нужный тип данных или функцию.

Для проекта сначала прочитайте сообщение предпусковой диагностики. Оно указывает конкретного агента и критичную настройку, если запуск заблокирован.

Если проблема повторяется, включите диагностический лог, повторите проблему один раз и сохраните журнал.

Не повторяйте много раз подряд платный запрос, если приложение сообщает о неопределённом сетевом сбое.
            """.trimIndent()
        )
    )

    val SECTIONS: List<String> = buildList {
        add(
            buildString {
                appendLine("# Памятка Umnik")
                appendLine()
                appendLine("Выберите нужный раздел. Памятка работает локально: открытие этих страниц не отправляет запросы в OpenRouter и не расходует баланс.")
                appendLine()
                appendLine("## Оглавление")
                appendLine()
                TOPICS.forEachIndexed { index, topic ->
                    appendLine("[${index + 1}. ${topic.title}](umnik://guide/${index + 1})")
                }
                appendLine()
                appendLine("Внутри каждой главы есть ссылка **← К оглавлению**.")
            }.trim()
        )

        TOPICS.forEach { topic ->
            add(
                buildString {
                    append(topic.body.trim())
                    appendLine()
                    appendLine()
                    append("[← К оглавлению](umnik://guide/0)")
                }
            )
        }
    }
}
