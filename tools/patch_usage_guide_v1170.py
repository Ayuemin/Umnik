from pathlib import Path

path = Path("app/src/main/java/com/ayuemin/ymnik/help/UmnikUsageGuide.kt")
text = path.read_text(encoding="utf-8")
old = '''        Topic(
            "База знаний чата",
'''
insert = '''        Topic(
            "Оркестратор и параллельная работа",
            """
# Оркестратор и параллельная работа

**Что это:** чат **◆ Оркестратор** управляет другими чатами проекта. Ему можно дать задачу обычным языком, а Umnik запустит нужных исполнителей и сохранит результаты в их собственных чатах.

**Как выполняются шаги:** независимые исполнители могут работать одновременно. Если следующему шагу нужен результат предыдущего, такая часть выполняется последовательно. Искусственного деления исполнителей на группы по три нет.

**Важно:** внутри одного и того же чата одновременно выполняется не более одного верхнеуровневого запроса. Поэтому если специалист уже отвечает на ваш ручной запрос, Оркестратор не начнёт вторую работу в этом же чате и не смешает истории.

**Одинаковые модели:** разные чаты могут одновременно использовать одну и ту же модель через OpenRouter. Umnik не вводит собственного фиксированного лимита параллельных чатов; фактические ограничения зависят от OpenRouter, выбранной модели, сети и устройства.

**Пример:** попросите Оркестратор одновременно дать Исследователю найти факты, а Аналитику разобрать исходный материал. После завершения обоих можно передать их результаты Автору, поэтому первые две работы пойдут параллельно, а Автор начнёт после получения нужного контекста.
            """.trimIndent()
        ),
        Topic(
            "База знаний чата",
'''
count = text.count(old)
if count != 1:
    raise SystemExit(f"expected insertion point once, got {count}")
path.write_text(text.replace(old, insert, 1), encoding="utf-8")
print("usage guide updated for v1.17.0")
